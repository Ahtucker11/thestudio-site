---
interface Props {
  label?: string;
}

const { label = "NOTE" } = Astro.props;
---

<div class="field-note">
  <span class="field-note-icon">{label}</span>
  <span class="field-note-text"><slot /></span>
</div>
