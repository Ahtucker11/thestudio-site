---
import { cn } from "../../lib/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn("tex-content mx-auto w-full max-w-[1320px] px-6 md:px-10 lg:px-12", className)}>
  <slot />
</div>
